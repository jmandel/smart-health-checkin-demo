node_modules
.DS_Store
package-lock.json

# Note: dist/ (library) is checked in so npm install from GitHub works without bun

# Demo app build outputs (intermediate, not needed)
demo/requester/dist/
demo/checkin/dist/
demo/source-flexpa/dist/

# Static site build output (generated by build.ts)
build/

# Test outputs
test-results/
playwright-report/
test_output.txt
